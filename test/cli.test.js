const expect = require('chai').expect;

describe('CLI', () => {
  describe('Parameters', () => {
    it('Base file should be mandatory');
    it('Mixin file should be mandatory');
    it('Selector should be mandatory');
    it('Output file should be mandatory');
  });

  it('Should read, insert and write yaml');
});

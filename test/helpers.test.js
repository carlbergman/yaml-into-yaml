const expect = require('chai').expect;

describe('Helpers', () => {
  it('Should throw if reading invalid yaml');
  it('Should throw if writing invalid yaml');
  it('Should throw if file not found');
  it('Should throw if selector not found');
  it('Should insert object into object');
});
